# Rebuild Relay at capability seams

Route-facing controllers return display-ready models and semantic actions through capability-specific ports. Local, sample, Convex, and in-memory adapters satisfy those ports so presentation code does not branch on storage mode or expose one large Workspace interface.
