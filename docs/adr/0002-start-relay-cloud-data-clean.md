# Start Relay cloud data clean

The Relay rebuild writes new cloud records and leaves old product tables and records unread, unmigrated, and undeleted. This keeps the old data recoverable while the replacement starts from a model built for Relay.
